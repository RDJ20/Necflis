<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.necflis">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainoActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".cuenta"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".series"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".peliculas"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".cambiarplan"
            android:theme="@style/inicioSesion" />
        <activity android:name=".busqueda" />
        <activity
            android:name=".pagos"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".planes"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".datospersonales"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".emailcontrasena"
            android:theme="@style/inicioSesion" />
        <activity
            android:name=".Splash"
            android:theme="@style/FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity1"
            android:theme="@style/inicioSesion" />
    </application>

</manifest>